{
  "name": "fuseki-shogi-web",
  "private": true,
  "type": "module",
  "license": "GPL-3.0-only",
  "description": "ブラウザ完結版の布石将棋（静的配信）",
  "scripts": {
    "build": "node build.mjs",
    "build:local": "node build.mjs --with-model",
    "watch": "node build.mjs --watch --with-model",
    "serve": "node serve.mjs",
    "start": "node build.mjs --with-model && node serve.mjs"
  },
  "dependencies": {
    "@mizarjp/yaneuraou.k-p": "^7.6.3-alpha.0",
    "onnxruntime-web": "^1.29.0",
    "shogiground": "^0.10.0",
    "shogiops": "^0.21.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
